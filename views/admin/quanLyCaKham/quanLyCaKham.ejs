<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản Lý Ca Khám & Lịch Trực</title>
    
    <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/vendor/fontawesome/css/all.min.css">
    
    <link rel="stylesheet" href="/vendor/flatpickr/flatpickr.min.css">
    <script src="/vendor/flatpickr/flatpickr.min.js"></script>
    <script src="/vendor/flatpickr/l10n/vn.js"></script>    
<link rel="stylesheet" href="/css/admin/sidebar.css">
    <link rel="stylesheet" href="/css/admin/style.css">
    <link rel="stylesheet" href="/css/admin/quanLyCaKham/quanLyCaKham.css">

    <!-- Giao diện thông báo Bootstrap dùng chung -->
    <link rel="stylesheet" href="/css/migrated-inline.css?v=1">
    <link rel="stylesheet" href="/css/bootstrap-ui.css?v=2">
    <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="/js/bootstrap-ui.js?v=2"></script>
</head>
<body>
    <% if (typeof uiMessage !== 'undefined' && uiMessage && uiMessage.message) { %>
      <template
        data-app-message="<%= uiMessage.message %>"
                data-app-type="<%= uiMessage.type || 'info' %>"
                data-app-title="<%= uiMessage.title || 'Thông báo' %>"></template>
    <% } %>


    <div class="d-flex vh-100 overflow-hidden csp-admin-quanlycakham-quanlycakham-ejs-16">
        
        <%- include('../sidebar') %>
        
        <div class="main-content flex-grow-1 p-3 p-md-4 overflow-auto">
            
            <div class="card-box bg-white p-4 rounded shadow-sm border-0">
                
                <div class="card-header-title">
                    <h2>Quản Lý Lịch Trực Bác Sĩ</h2>
                </div>

                <div class="week-navigator d-flex flex-column flex-lg-row align-items-center justify-content-between gap-3 mb-4 p-3 border rounded">
                    
                    <div class="week-controls d-flex align-items-center justify-content-center gap-2 w-100 w-lg-auto">
                        <button type="button" class="btn-import js-change-week" data-week-offset="-7">
                            <i class="fas fa-chevron-left"></i> <span class="d-none d-sm-inline">Tuần trước</span>
                        </button>
                        
                        <div class="date-input-wrapper mx-2">
                            <div class="week-info">
                                <i class="far fa-clock"></i> <%= typeof weekLabel !== 'undefined' ? weekLabel : 'Đang tải...' %>
                            </div>
                            <input type="text" name="week" id="weekInput" 
                                   value="<%= typeof currentInputVal !== 'undefined' ? currentInputVal : '' %>" 
                                   class="file-input-custom datepicker mx-auto d-block csp-admin-quanlycakham-quanlycakham-ejs-15" 
                                   
                                   placeholder="Chọn ngày">
                            <small class="csp-admin-quanlycakham-quanlycakham-ejs-14">(Chọn ngày)</small>
                        </div>

                        <button type="button" class="btn-import js-change-week" data-week-offset="7">
                            <span class="d-none d-sm-inline">Tuần sau</span> <i class="fas fa-chevron-right"></i>
                        </button>
                    </div>

                    <form action="/admin/importExcel" method="POST" enctype="multipart/form-data" class="upload-section m-0 w-100 w-lg-auto justify-content-center flex-nowrap">
                         <input type="file" name="excelFile" class="file-input-custom flex-grow-1 flex-lg-grow-0 csp-admin-quanlycakham-quanlycakham-ejs-13" accept=".xlsx, .xls" required>
                         <button type="submit" class="btn-import text-nowrap"><i class="fas fa-upload"></i> Import</button>
                    </form>
                </div>

                <div class="table-responsive">
                    <table class="custom-table w-100">
                        <thead>
                            <tr>
                                <th class="csp-admin-quanlycakham-quanlycakham-ejs-12">NGÀY TRỰC</th>
                                <th class="csp-admin-quanlycakham-quanlycakham-ejs-11">CHUYÊN KHOA</th>
                                <th class="csp-admin-quanlycakham-quanlycakham-ejs-10">BÁC SĨ TRỰC</th>
                                <th class="csp-admin-quanlycakham-quanlycakham-ejs-9">THAO TÁC</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (typeof weekData !== 'undefined' && weekData.length > 0) { %>
                                <% weekData.forEach((dayGroup) => { %>
                                    <% dayGroup.shifts.forEach((shift, index) => { %>
                                        <tr>
                                            <% if (index === 0) { %>
                                                <td rowspan="<%= dayGroup.shifts.length %>" class="csp-admin-quanlycakham-quanlycakham-ejs-8">
                                                    <i class="far fa-calendar-alt"></i> <br class="d-md-none"> <%= dayGroup.dateDisplay %>
                                                </td>
                                            <% } %>

                                            <td><span class="badge-khoa"><%= shift.khoa %></span></td>
                                            
                                            <td class="csp-admin-quanlycakham-quanlycakham-ejs-7">
                                                <%= shift.tenBacSi %>
                                            </td>

                                            <td class="action-cols text-center">
                                                <div class="action-btn-container"> 
                                                    <% 
                                                        let today = new Date();
                                                        today.setHours(0,0,0,0);
                                                        let shiftDate = new Date(shift.ngay); 
                                                        shiftDate.setHours(0,0,0,0);
                                                    %>

                                                    <% if (shiftDate >= today) { %>
                                                        <button type="button" class="btn-edit-icon js-prepare-edit-modal"
                                                                data-bs-toggle="modal"
                                                                data-bs-target="#editModal"
                                                                data-id-ca-kham="<%= shift.id %>"
                                                                data-id-khoa="<%= shift.idKhoa || '' %>"
                                                                data-ten-bac-si="<%= shift.tenBacSi %>"
                                                                data-ten-khoa="<%= shift.khoa %>">
                                                            <i class="fas fa-edit"></i> Sửa
                                                        </button>
                                                    <% } else { %>
                                                        <span class="csp-admin-quanlycakham-quanlycakham-ejs-6"><i class="fas fa-lock"></i> Đã khóa</span>
                                                    <% } %>
                                                </div>
                                            </td>
                                        </tr>
                                    <% }) %>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="4" class="csp-admin-quanlycakham-quanlycakham-ejs-5">
                                        Không có lịch trực nào trong tuần này.
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="editModal" tabindex="-1" aria-labelledby="editModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header border-bottom">
                    <h5 class="modal-title fw-bold csp-admin-quanlycakham-quanlycakham-ejs-4" id="editModalLabel">Đổi Bác Sĩ Trực</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form action="/admin/updateCaTruc" method="POST">
                    <div class="modal-body">
                        <input type="hidden" name="id_caKham" id="modalIdCaKham">
                        
                        <div class="form-group mb-3">
                            <label class="fw-bold mb-1">Chuyên khoa:</label>
                            <input type="text" id="modalTenKhoa" class="form-control csp-admin-quanlycakham-quanlycakham-ejs-3" readonly>
                        </div>

                        <div class="form-group mb-3">
                            <label class="fw-bold mb-1">Bác sĩ hiện tại:</label>
                            <input type="text" id="modalTenBacSiCu" class="form-control csp-admin-quanlycakham-quanlycakham-ejs-2" readonly>
                        </div>

                        <div class="form-group mb-3">
                            <label class="fw-bold mb-1 csp-admin-quanlycakham-quanlycakham-ejs-1">Chọn Bác sĩ thay thế:</label>
                            <select name="id_bacSiMoi" id="modalSelectBacSi" class="form-select" required>
                                <option value="">Đang tải danh sách...</option>
                            </select>
                        </div>
                    </div>
                    <div class="modal-footer border-top-0">
                        <button type="button" class="btn-cancel" data-bs-dismiss="modal">Hủy</button>
                        <button type="submit" class="btn-save">Lưu Thay Đổi</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
<script nonce="<%= cspNonce %>">
        document.addEventListener("DOMContentLoaded", function() {
            document.querySelectorAll('.js-change-week').forEach(function (button) {
                button.addEventListener('click', function () {
                    changeWeek(Number(this.dataset.weekOffset || 0));
                });
            });

            document.querySelectorAll('.js-prepare-edit-modal').forEach(function (button) {
                button.addEventListener('click', function () {
                    prepareEditModal(
                        this.dataset.idCaKham || '',
                        this.dataset.idKhoa || '',
                        this.dataset.tenBacSi || '',
                        this.dataset.tenKhoa || ''
                    );
                });
            });

            flatpickr(".datepicker", {
                locale: "vn",
                dateFormat: "Y-m-d",
                altInput: true,
                altFormat: "d/m/Y",
                allowInput: true,
                onChange: function(selectedDates, dateStr, instance) {
                    if (dateStr) {
                        window.location.href = `/admin/quanLyCaKham?week=${dateStr}`;
                    }
                }
            });
        });

        function changeWeek(offsetDays) {
            const input = document.getElementById('weekInput');
            let currentDate = new Date();
            if (input.value) {
                const parts = input.value.split('-');
                if (parts.length === 3) {
                    currentDate = new Date(parts[0], parts[1] - 1, parts[2]);
                }
            }
            currentDate.setDate(currentDate.getDate() + offsetDays);
            const y = currentDate.getFullYear();
            const m = String(currentDate.getMonth() + 1).padStart(2, '0');
            const d = String(currentDate.getDate()).padStart(2, '0');
            window.location.href = `/admin/quanLyCaKham?week=${y}-${m}-${d}`;
        }

        async function prepareEditModal(idCaKham, idKhoa, tenBacSi, tenKhoa) {
            document.getElementById('modalIdCaKham').value = idCaKham;
            document.getElementById('modalTenBacSiCu').value = tenBacSi;
            document.getElementById('modalTenKhoa').value = tenKhoa;
            
            const selectBox = document.getElementById('modalSelectBacSi');
            selectBox.innerHTML = '<option value="">Đang tải danh sách...</option>';

            if (!idKhoa || idKhoa === 'null') {
                selectBox.innerHTML = '<option value="">Khoa không hợp lệ</option>';
                return;
            }

            try {
                const res = await fetch(`/admin/api/get-bacsi-by-khoa/${idKhoa}`);
                const data = await res.json();
                const doctors = data.data ? data.data : data;

                selectBox.innerHTML = '';
                if (!Array.isArray(doctors) || doctors.length === 0) {
                    selectBox.innerHTML = '<option value="">Không có bác sĩ khác</option>';
                    return;
                }

                doctors.forEach(doc => {
                    const option = document.createElement('option');
                    option.value = doc.id;
                    option.text = doc.hoTen;
                    if(doc.hoTen === tenBacSi) option.selected = true;
                    selectBox.appendChild(option);
                });
            } catch (err) {
                selectBox.innerHTML = '<option>Lỗi tải dữ liệu</option>';
            }
        }
    </script>

    <% if (typeof msg !== 'undefined' && msg) { %>
        <% const decodedMsg = (() => { try { return decodeURIComponent(msg); } catch (error) { return msg; } })(); %>
        <% const isErrorMessage = decodedMsg.toLowerCase().includes('lỗi') || decodedMsg.toLowerCase().includes('không thể') || decodedMsg.toLowerCase().includes('error'); %>
        <template
            data-app-message="<%= decodedMsg %>"
            data-app-type="<%= isErrorMessage ? 'error' : 'success' %>"
            data-app-title="<%= isErrorMessage ? 'Thông báo' : 'Thành công' %>"
            data-app-mode="dialog"></template>
    <% } %>
</body>
</html>